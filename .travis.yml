language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-dummytest' ]; then cd .. && eval "mv $currentfolder generator-dummytest" && cd generator-dummytest; fi
